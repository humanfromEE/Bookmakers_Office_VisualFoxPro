user_answer = MESSAGEBOX("Ви дійсно бажаєте перезапустити файл проекту?", 1 + 32 + 256, "ПРОЦЕС ПЕРЕЗАПУСКУ ФАЙЛА ПРОЕКТУ")
IF (user_answer = 1) THEN
	CLOSE TABLES ALL
	_SCREEN.ActiveForm.release()
	CLOSE ALL 
	MODIFY PROJECT (_VFP.DefaultFilePath + "\project.pjx")
	DO FORM (_VFP.DefaultFilePath + "\main.scx")
ENDIF
