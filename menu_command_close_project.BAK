user_answer = MESSAGEBOX("Ви дійсно бажаєте закрити ввесь проект?", 4 + 32 + 256, "ПРОЦЕС ЗАКРИВАННЯ УСЬОГО ПРОЕКТУ")
IF (user_answer = 6) THEN
	user_answer = MESSAGEBOX("УВАГА! Ви зараз знаходитесь у Project Manager? Якщо ви хочете просто вийти з EXE-програми, то оберіть пункт ''Вийти з VFP''", 4 + 48 + 256, "ПРОЦЕС ЗАКРИВАННЯ УСЬОГО ПРОЕКТУ")
	IF (user_answer = 6) THEN
		user_answer = MESSAGEBOX("Якщо ви закриєте проект через EXE-файл, ви втратите дані й не зможете закрити виконавче вікно VFP.  Якщо ви хочете просто вийти з EXE-програми, то оберіть пункт ''Вийти з VFP''. Ви дійсно впевнені, що закриваєте проект через Project Manager?", 1 + 16 + 256, "ПРОЦЕС ЗАКРИВАННЯ УСЬОГО ПРОЕКТУ")
		IF (user_answer = 1) THEN
			CLOSE TABLES ALL 
			_SCREEN.ActiveForm.release()
			CLOSE ALL
		ENDIF
	ENDIF 
ENDIF